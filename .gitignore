# =====================================================================
# Sensitive — NEVER COMMIT
# =====================================================================
bots.json
settings.json
*.token
*.secret

# Local grab history (SQLite) — user data, never commit
grabs.db
*.db-wal
*.db-shm

# =====================================================================
# User-machine specific (paths inside)
# =====================================================================
Selfbot Manager.lnk

# =====================================================================
# Python
# =====================================================================
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
*.egg
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml

# PyInstaller build artifacts (selfbot-manager.spec itself IS tracked)
*.manifest
*.toc
*.pkg

# Auto-generated at build time (tools/build.py) — never commit
sofi_manager/_build_info.py

# Build outputs (never commit)
*.exe
*.zip

# Virtual environments
env/
venv/
.venv/
ENV/

# =====================================================================
# IDE / editors / agents
# =====================================================================
.vscode/
.idea/
.claude/
.claude/worktrees/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# =====================================================================
# Logs / runtime
# =====================================================================
*.log
*.bak
*.tmp
