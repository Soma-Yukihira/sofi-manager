"""Single source of truth for the application version.

Read by `updater.py` and surfaced in the GUI. Keep the format `MAJOR.MINOR.PATCH`
(SemVer); the GitHub release tag may be prefixed with `v` (e.g. `v0.1.0`).
"""

__version__ = "0.1.0"
__repo__ = "Soma-Yukihira/sofi-manager"
